# Backend environment variables
# These should be set in AWS ECS task definition or docker-compose

# MongoDB connection
MONGO_URI=mongodb://database:27017/kudosboard

# Server port
PORT=5000

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=*

# Node environment
NODE_ENV=production
